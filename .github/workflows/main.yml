name: Trigger Jenkins Job on App Changes

on:
  push:
    paths:
      - 'application/**' # Trigger only when files in the app folder are modified

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://128.203.96.19:8080/job/observability/build?token=test"
